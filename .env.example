# Local Postgres (Homebrew: `brew services start postgresql@16`, then `createdb menucraft`)
# URL format: postgresql://<mac-username>@localhost:5432/menucraft
DATABASE_URL=postgresql://huseyin@localhost:5432/menucraft

# SHOPIFY_API_KEY / SHOPIFY_API_SECRET / SCOPES are injected by `shopify app dev`
# in development. Set them explicitly for production deploys.
